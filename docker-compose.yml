version: '3.8'

services:
  web:
    build: .
    ports:
      - "1234:80"
    volumes:
      # Optional: Mount the public directory for live updates during development
      # Remove or comment out for a production-like build from Dockerfile contents
      - ./public:/usr/share/nginx/html:ro
    restart: unless-stopped
